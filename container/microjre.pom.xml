<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <packaging>rpm</packaging>
  <modelVersion>4.0.0</modelVersion>
  <artifactId>rlp_07-microjre</artifactId>
  <version>${revision}${sha1}${changelist}</version>
  <name>rlp_07_microjre</name>
  <description>rlp_07-microjre</description>
  <groupId>com.teragrep</groupId>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.target>25</maven.compiler.target>
    <java.version>25</java.version>
    <revision>0.0.1</revision>
    <changelist>-SNAPSHOT</changelist>
    <sha1/>
  </properties>
  <build>
    <directory>${project.basedir}/target</directory>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skipTests>true</skipTests>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.1.1</version>
        <executions>
          <execution>
            <id>jdeps</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <!-- produces nohup.out -->
              <executable>/usr/bin/bash</executable>
              <arguments>
                <argument>-c</argument>
                <argument>/usr/lib/jvm/java-25-openjdk/bin/jdeps --multi-release=25 -quiet -print-module-deps --ignore-missing-deps --recursive /opt/teragrep/rlp_07/lib/rlp_07.jar > modules.txt</argument>
              </arguments>
              <workingDirectory>${project.build.directory}/${project.artifactId}</workingDirectory>
            </configuration>
          </execution>
          <execution>
            <id>jlink</id>
            <phase>process-sources</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>/usr/bin/xargs</executable>
              <arguments>
                <argument>-a</argument>
                <argument>modules.txt</argument>
                <argument>/usr/lib/jvm/java-25-openjdk/bin/jlink</argument>
                <argument>--compress=zip-9</argument>
                <argument>--no-header-files</argument>
                <argument>--no-man-pages</argument>
                <argument>--strip-debug</argument>
                <argument>--output=${project.artifactId}</argument>
                <argument>--add-modules</argument>
              </arguments>
              <workingDirectory>${project.build.directory}/${project.artifactId}</workingDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>rpm-maven-plugin</artifactId>
        <version>2.2.0</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>default-rpm</id>
            <goals>
              <goal>rpm</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <summary>${project.groupId}-${project.artifactId}</summary>
          <name>${project.groupId}-${project.artifactId}</name>
          <version>${project.version}</version>
          <release>${env.BUILD_ID}</release>
          <license>Apache-1.1 AND Apache-2.0 AND LicenseRef-Callaway-BSD AND LicenseRef-Callaway-BSD-with-advertising AND GPL-1.0-or-later AND GPL-2.0-only AND LicenseRef-Callaway-GPLv2-with-exceptions AND IJG AND LicenseRef-Callaway-LGPLv2+ AND LicenseRef-Callaway-MIT AND MPL-2.0 AND LicenseRef-Callaway-Public-Domain AND W3C AND Zlib AND ISC AND FTL AND LicenseRef-RSA</license>
          <distribution>teragrep Log Management Suite</distribution>
          <vendor>https://teragrep.com/</vendor>
          <packager>teragrep &lt;servicedesk@teragrep.com&gt;</packager>
          <group>teragrep/LogManagementSuite</group>
          <description>${project.groupId}-${project.artifactId}</description>
          <needarch>true</needarch>
          <defaultUsername>root</defaultUsername>
          <defaultGroupname>root</defaultGroupname>
          <defaultFilemode>0644</defaultFilemode>
          <defaultDirmode>0755</defaultDirmode>
          <keyname>${env.RPM_SIGNING_KEYNAME}</keyname>
          <keyPassphrase>
            <passphrase>${env.RPM_SIGNING_PASSPHRASE}</passphrase>
          </keyPassphrase>
          <defineStatements>
            <defineStatement>_build_id_links none</defineStatement>
            <defineStatement>__provides_exclude ^osgi\\(.*$</defineStatement>
            <defineStatement>__requires_exclude ^osgi\\(.*$</defineStatement>
          </defineStatements>
          <mappings>
            <mapping>
              <directory>/opt/teragrep/${project.artifactId}</directory>
              <directoryIncluded>true</directoryIncluded>
              <filemode>755</filemode>
              <dirmode>755</dirmode>
              <username>root</username>
              <groupname>root</groupname>
              <recurseDirectories>true</recurseDirectories>
              <sources>
                <source>
                  <location>${project.build.directory}/${project.artifactId}/${project.artifactId}</location>
                  <destination>.</destination>
                </source>
              </sources>
            </mapping>
          </mappings>
          <provides>
            <provide>java-25-openjdk-headless</provide>
          </provides>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>2.8.2</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <executions>
          <execution>
            <id>default-compile</id>
            <phase>compile</phase>
            <goals>
              <goal>compile</goal>
            </goals>
            <configuration>
              <skipMain>true</skipMain>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
